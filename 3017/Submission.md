รับ input มา และแทนว่าตัวแปร pay 
กำหนดให้ service เป็นตัวแปรที่เก็บค่าบริการมีค่าเป็น 10% ของค่าใช้จ่าย หรือคือ ค่าใช้จ่าย * 0.10 
เนื่องจากโจทย์กำหนดให้ค่าบริการมีค่าเป็นอย่างน้อย 50 และ มีค่าไม่เกิน 1000 จึงได้กำหนดเงื่อนไขขึ้นโดยใช้ if elif ว่า หากคิดค่า servie ออกมาแล้วไม่ถึง 50 ให้คิดเป็น 50 เลย เช่นเดียวกับ 1000 หากเกิน 1000 ให้คิดเป็น 1000 
สุดท้ายค่า vat โจทย์กำหนดให้คิดเป็น 7% จาก ค่าใช้จ่าย + กับค่าบริการเรียบร้อยแล้ว เลยออกมาเป็นการนำตัวแปร (pay + service) * ด้วย 7% หรือ 0.07 
พอได้ค่าครบแล้ว นำค่าทุกอย่างที่ได้มาบวกกัน กำหนดเป็นตัวแปร total ที่นำค่าใช้จ่าย + ค่าบริการ + ค่าvat 
print ตัวแปร total ออกมา และโจทย์ให้คิดเป็นทศนิยม 2 ตัวแหน่งจึงใช้ :.2f ไปด้วย
